name: Run Winter Threat Script

on:
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true   # ensures JS actions run on Node 24

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Cache pip dependencies
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements_winter_threat.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: pip install -r requirements_winter_threat.txt

      - name: Create credentials.json
        run: echo "${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}" | base64 --decode > credentials.json

      - name: Run Winter Threat Script (with retry and live logs)
        run: |
          success=0
          for i in 1 2 3; do
            echo "Attempt $i..."
            # -u for unbuffered output so logs appear live
            if python -u winter_threat.py; then
              success=1
              break
            fi
            echo "Attempt $i failed. Retrying in 20 seconds..."
            sleep 20
          done

          if [ $success -ne 1 ]; then
            echo "All attempts failed."
            exit 1
          fi
